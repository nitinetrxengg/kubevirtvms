---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: rhel85dcdrpvc
  labels:
    appname: rhel85dcdrpvc
spec:
  accessModes:
  - ReadWriteMany
  storageClassName: ocs-storagecluster-ceph-rbd
  volumeMode: Block
  resources:
    requests:
      storage: 12Gi
